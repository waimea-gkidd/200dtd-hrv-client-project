{#=====================================================
  Home page features (from figma models): 
  - Shows how many clients there are
  - Shows the 5 most recent clients
  - Link to go to Clients page
=====================================================#}

{% extends 'pages/base.jinja' %}

{% block title %}
  HRV Client Tracker
{% endblock %}

{% block content %}

  <h1>Home - HRV Client Tracker</h1>

  <p>Total clients: {{ client_count }}</p>

  <h2>Recent Clients</h2>

  <ul>
    {% for c in clients %}
      <li>{{ c.name }} // {{ c.phone }} // {{ c.email }} // {{ c.status }} // {{ c.notes }}</li>
    {% else %}
      <li>You have no clients.</li>
    {% endfor %}
  </ul>

  <a href="/clients/">Go to Clients</a>
  
{% endblock %}
